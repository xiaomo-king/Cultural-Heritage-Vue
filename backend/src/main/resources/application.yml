server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jiangyou_shiyi?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 864656297
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

# 文件上传路径
file:
  upload-path: ./uploads
  access-path: /uploads/**

# JWT 配置
jwt:
  secret: JiangYouShiYi2026SecretKeyForJWT!@#$%^&*()AdminPortal
  expiration: 86400000
